# Copy to .env and fill in real values

# MySQL (Sered)
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=pacfgm_quest
DB_NAME_TEST=pacfgm_quest_test
DB_PORT=3306

# JWT (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=

# Google Gemini API (get at: aistudio.google.com)
GEMINI_API_KEY=

# App
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
